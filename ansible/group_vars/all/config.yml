---
environments:
  - development
  - production

# server vars
server_name: localhost

# base vars
package_bundles:
packages:

# ownership vars
app_name: sample_app
user_name: "{{ 'vagrant' if target == 'virtualbox' else app_name }}"
group_name: "{{ user_name }}"

# apt vars
use_apt_proxy: false
